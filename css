body {
    font-family: Arial, sans-serif;
}

form {
    max-width: 900px;
    margin: 50px auto;
    border: 1px solid #e23434;
    
}

label, input, select {
    display: block;
    margin-bottom: 10px;
}

button {
    background-color: #e0d6d0;
    color: rgb(8, 6, 6);
    padding: 10px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

button:hover {
    background-color: #a07145;
}
